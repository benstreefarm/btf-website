<nav class="navbar">
  <div class="logo">Ben's Christmas Tree Farm</div>
  <ul class="nav-links">
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/trees">Our Trees</a></li>
    <li><a href="/visit">Visit</a></li>
    <li><a href="/contact">Contact</a></li>
  </ul>
</nav>
<style>
  .navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    background: rgba(26, 46, 26, 0.8);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--pine-accent);
    position: sticky;
    top: 0;
    z-index: 1000;
    /* This is the key: allow items to wrap to a new line if they don't fit */
    flex-wrap: wrap; 
  }

  .logo {
    font-family: Georgia, serif;
    font-weight: bold;
    font-size: 1.5rem;
    color: var(--cream);
    text-decoration: none;
    /* Ensure the logo doesn't get squashed */
    flex: 1 1 auto;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-links a {
    color: var(--cream);
    text-decoration: none;
    font-weight: 500;
    text-transform: uppercase;
    font-size: 0.9rem;
    letter-spacing: 1px;
  }

  /* Mobile-Specific Adjustment */
  @media (max-width: 600px) {
    .navbar {
      justify-content: center;
      gap: 1rem;
      padding: 1rem;
    }

    .logo {
      text-align: center;
      width: 100%; /* Forces the logo to take its own line */
      margin-bottom: 0.5rem;
    }

    .nav-links {
      gap: 1rem;
      justify-content: center;
      width: 100%;
    }
    
    .nav-links a {
      font-size: 0.8rem; /* Slightly smaller text for tight spaces */
    }
  }
  /* TARGET MOBILE DEVICES BELOW 480PX WIDE (eliminate line wrapping on title and nav bar etc 5/21/26) */
  @media (max-width: 480px) {
    /* Compress the main branding title text size slightly */
    nav h1, .nav-brand-title { 
        font-size: 1.5rem !important; 
        white-space: nowrap; /* Forces the business name onto exactly one line */
        letter-spacing: -0.5px;
    }

    /* Target the link container row */
    .nav-links-container, nav ul {
        display: flex !important;
        justify-content: space-between !important;
        gap: 0.5rem !important; /* Shrinks the massive space between menu words */
        padding: 0 0.5rem !important;
    }

    /* Target individual menu text links */
    .nav-links-container a, nav ul li a {
        font-size: 0.8rem !important; /* Scales text down just enough to preserve single-line flow */
        white-space: nowrap; /* Guarantees "OUR TREES" never breaks into a stack */
        letter-spacing: 0px;
    }
}

/* THE FLUID BRAND TITLE ENGINE */
.nav-brand-title, nav h1 {
    /* clamp(MIN, PREFERRED, MAX)
       - Min: Never shrinks smaller than 1.4rem (perfectly readable on an iPhone SE)
       - Preferred: Scales dynamically at 5% of the total viewport width
       - Max: Cap it at 2.2rem so it doesn't get oversized on a 4K desktop monitor
    */
    font-size: clamp(1.4rem, 5vw, 2.2rem) !important;
    white-space: nowrap; /* Rigid rule: Absolutely forbids the text from splitting into two lines */
    letter-spacing: -0.5px; /* Slightly tightens letter tracking on mobile for a premium editorial look */
    overflow: hidden;
    text-overflow: ellipsis; /* Fallback: If a tiny screen somehow runs out of pixels, it softens the edge gracefully */
}
</style>